# Entrypoint file 
import mean_var_std
from unittest import main

# Printing the result of the calculate() function when it is passed a list of integers as an argument. Calculates the mean, variance, and standard deviation of the list of numbers
print(mean_var_std.calculate([0,1,2,3,4,5,6,7,8]))
